# Atlas Installer

Reserved for the trusted local component that safely installs, updates, validates,
rolls back, and uninstalls Skills through host adapters.

The CLI/local service/extension shape and invocation protocol remain open. Do not
add an implementation before ADR approval. Follow `docs/standards/installer.md`.
