# Contract Tests

Cross-runtime compatibility tests for metadata, API, installer protocol,
artifacts, events, and host capabilities live here when scaffolding exists.
Slice-local contract definitions begin under `docs/05-design/contracts/` and
graduate here as executable shared contract tests when runtimes exist.
