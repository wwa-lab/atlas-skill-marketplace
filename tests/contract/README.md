# Contract Tests

Cross-runtime compatibility tests for metadata, API, installer protocol,
artifacts, events, and host capabilities live here when scaffolding exists.
Feature-local contract fixtures may begin in the feature's `specs/.../contracts/`
directory and graduate here when they become executable shared tests.
