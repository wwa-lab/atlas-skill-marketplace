# Infrastructure

Local and deployment infrastructure belongs here after ADRs select the runtime,
persistence, async job, artifact, identity, and deployment topology. Do not add
speculative Compose, Kubernetes, cloud, or database configuration.
